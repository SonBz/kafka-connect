# FROM confluentinc/cp-kafka-connect:7.6.0

# USER root
# # Install Debezium Oracle Connector
# RUN confluent-hub install --no-prompt debezium/debezium-connector-oracle:2.5.0

# Add ojdbc8 if needed (Debezium usually includes it, but sometimes we need to be explicit or if using XStream)
# For LogMiner (default in recent Debezium), the driver included should suffice.
